

class ModelManager:
    def __init__(self, main_engine):
        self.engine = main_engine
        self.loader = self.engine.loader

    def spawn(self, model_path, snap_to_grid=True):
        pass
